# Points the web app at the API. 127.0.0.1 (not "localhost") avoids a
# well-known Windows delay where browsers try IPv6 (::1) first and time out
# before falling back to IPv4.
VITE_API_URL=http://127.0.0.1:4000
